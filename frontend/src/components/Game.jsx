import { useNavigate, Link } from "react-router-dom";

function Game() {
  return (
    <div>
      <h1>Where's Maverick (A photo tagging Game)</h1>
      <div>
        <img 
        />
      </div>
      <Link to="/">Home</Link>
      <div></div>
    </div>
  );
}

export default Game;
